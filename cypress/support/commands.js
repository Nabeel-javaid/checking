// Cypress.Commands.add('enableWindowEthereum', () => {
//     cy.window().then((win) => {
//       if (win.ethereum === undefined) {
//         cy.log('window.ethereum is not defined. Skipping stubbing.');
//       } else {
//         cy.stub(win, 'ethereum').value({});
//       }
//     });
//   });
  